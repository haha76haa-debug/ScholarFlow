---
type: paper
project: {{projectSlug if projectSlug else "zotero_obsidian_kb"}}
title: "{{title if title else 'Untitled'}}"
citekey: "{{citekey if citekey else (citationKey if citationKey else 'paper')}}"
zotero_key: "{{key if key else (zoteroKey if zoteroKey else '')}}"
canvas_visibility: visible
status: to-read
source_type: {{ "preprint" if publicationTitle == "arXiv" or itemType == "preprint" else ("book" if itemType == "book" else ("conference paper" if itemType == "conferencePaper" else ("journal article" if itemType == "journalArticle" else "full paper"))) }}
claim_strength: observed
authors:
{%- if authors and authors.length > 0 %}
{%- for author in authors %}
  - "{{author.family if author.family else (author.lastName if author.lastName else (author.name if author.name else author))}}, {{author.given if author.given else (author.firstName if author.firstName else '')}}"
{%- endfor %}
{%- elif creators and creators.length > 0 %}
{%- for creator in creators %}
  - "{{creator.lastName if creator.lastName else (creator.family if creator.family else creator.name)}}, {{creator.firstName if creator.firstName else (creator.given if creator.given else '')}}"
{%- endfor %}
{%- else %}
  - "Anonymous"
{%- endif %}
year: {{year if year else 2024}}
venue: "{{publicationTitle if publicationTitle else (conferenceName if conferenceName else (journal if journal else 'Preprint'))}}"
doi: "{{doi if doi else ''}}"
url: "{{url if url else ''}}"
keywords:
{%- if tags and tags.length > 0 %}
{%- for t in tags %}
  - "{{t.tag if t.tag else t}}"
{%- endfor %}
{%- else %}
  - "{{citekey if citekey else 'paper'}}"
{%- endif %}
concepts: []
methods: []
subfield: "{{subfield if subfield else 'general'}}"
related_papers: []
linked_knowledge:
  - "Knowledge/Literature Overview"
  - "Knowledge/Method Taxonomy"
  - "Knowledge/Research Gaps"
updated: 2026-08-19T00:00:00Z
---

# {{title}}
> **中文译名**：*(待补充中文翻译)*

---

## Claim
- **[EN]**: *Primary assertion, theoretical hypothesis, or principal empirical finding.*
- **[CN] 核心主张**：*(用中文概括本篇论文的核心论点与主要发现)*

---

## Research question
- **[EN]**: *What specific research problem, technical bottleneck, or scientific question does this paper investigate?*
- **[CN] 核心科学问题**：*(本论文旨在解决什么具体的技术痛点或前沿科学问题？)*

---

## Method
- **[EN] Methodology & Implementation**:
  1. *Core Architecture / Mechanism*: ...
  2. *Experimental Formulation / Protocol*: ...
- **[CN] 研究方法与实现路径**：
  1. *核心架构 / 原理机制*：...
  2. *实验设计与验证流程*：...

---

## Evidence
```md
Evidence ID: EVD-{{citekey if citekey else 'paper'}}-01
Source: [[Sources/Papers/{{citekey if citekey else 'paper'}}]]
Source type: full paper
Supports: "Primary claim of {{citekey if citekey else 'paper'}} / 核心结论支持主张"
Contradicts: ""
Method / dataset / metric: "{{publicationTitle if publicationTitle else (conferenceName if conferenceName else (journal if journal else ''))}}"
Limitation: ""
Project relevance: "Primary literature reference / 文献核心参考"
Claim strength: observed
```

---

## Strengths
- **Theoretical Rigor / 理论创新与深度**:
  - 
- **Empirical Validation / 实验效果与基准表现**:
  - 
- **Practical Impact / 实用价值与应用前景**:
  - 

---

## Limitation
- **Assumptions & Boundary Conditions / 假设前提与适用边界**:
  - 
- **Computational / Complexity Cost / 开销与实现成本**:
  - 
- **Unaddressed Challenges / 尚未解决的挑战**:
  - 

---

## Direct relevance to repo
- **[EN]**: *Actionable takeaways, reusable mechanisms, or theoretical foundations for our research.*
- **[CN] 本知识库直接应用价值**：*(这篇论文对我们当前的研究与知识体系有何直接启示或可借鉴之处？)*

---

## Relation to other papers
- **[EN]**: *Extends preceding foundational literature; introduces mechanisms adopted by subsequent works.*
- **[CN] 与其他文献的关联**：*(继承了哪些前人工作，又为后续哪些研究提供了基础？)*

---

## Knowledge links
- [[Knowledge/Literature Overview]]
- [[Knowledge/Method Taxonomy]]
- [[Knowledge/Research Gaps]]

---

## Key Annotations & Highlights
{%- if annotations and annotations.length > 0 %}
{%- for annotation in annotations %}
{%- if annotation.color == "#ffd400" or annotation.color == "yellow" %}
> [!quote]+ Motivation & Background (p. {{annotation.pageLabel if annotation.pageLabel else annotation.page}}) / 研究背景与动机
> {{annotation.annotatedText}}
{%- if annotation.comment %}
>
> **Note / 笔记**: {{annotation.comment}}
{%- endif %}
> [Open in Zotero]({{annotation.attachmentURI if annotation.attachmentURI else annotation.desktopURI}})

{%- elif annotation.color == "#5fb236" or annotation.color == "green" %}
> [!tip]+ Method & Implementation (p. {{annotation.pageLabel if annotation.pageLabel else annotation.page}}) / 核心方法与实现
> {{annotation.annotatedText}}
{%- if annotation.comment %}
>
> **Note / 笔记**: {{annotation.comment}}
{%- endif %}
> [Open in Zotero]({{annotation.attachmentURI if annotation.attachmentURI else annotation.desktopURI}})

{%- elif annotation.color == "#a28ae5" or annotation.color == "purple" %}
> [!note]+ Core Contribution & Claim (p. {{annotation.pageLabel if annotation.pageLabel else annotation.page}}) / 核心贡献与结论
> {{annotation.annotatedText}}
{%- if annotation.comment %}
>
> **Note / 笔记**: {{annotation.comment}}
{%- endif %}
> [Open in Zotero]({{annotation.attachmentURI if annotation.attachmentURI else annotation.desktopURI}})

{%- elif annotation.color == "#ff6666" or annotation.color == "red" %}
> [!warning]+ Limitation & Constraint (p. {{annotation.pageLabel if annotation.pageLabel else annotation.page}}) / 局限性与不足
> {{annotation.annotatedText}}
{%- if annotation.comment %}
>
> **Note / 笔记**: {{annotation.comment}}
{%- endif %}
> [Open in Zotero]({{annotation.attachmentURI if annotation.attachmentURI else annotation.desktopURI}})

{%- elif annotation.color == "#2ea8e5" or annotation.color == "blue" %}
> [!info]+ Benchmark & Key Reference (p. {{annotation.pageLabel if annotation.pageLabel else annotation.page}}) / 实验基准与关键参考
> {{annotation.annotatedText}}
{%- if annotation.comment %}
>
> **Note / 笔记**: {{annotation.comment}}
{%- endif %}
> [Open in Zotero]({{annotation.attachmentURI if annotation.attachmentURI else annotation.desktopURI}})

{%- else %}
> [!quote]+ General Highlight (p. {{annotation.pageLabel if annotation.pageLabel else annotation.page}}) / 高亮摘录
> {{annotation.annotatedText}}
{%- if annotation.comment %}
>
> **Note / 笔记**: {{annotation.comment}}
{%- endif %}
> [Open in Zotero]({{annotation.attachmentURI if annotation.attachmentURI else annotation.desktopURI}})

{%- endif %}
{%- endfor %}
{%- else %}
*No annotations extracted yet / 暂未提取到批注*
{%- endif %}
